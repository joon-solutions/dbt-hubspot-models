version: 2

models:
  - name: stg__outreach__account_opportunity
    description: Each record represents an Outreach account joined with opportunity
    columns:
      - name: id
        description: Unique ID for a pair of account_id and opportunity_id 
        tests:
          - unique
          - not_null
          - dbt_utils.at_least_one 
      
