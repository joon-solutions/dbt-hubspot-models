version: 2

models:
  - name: stg_hubspot__email_aggregate_status_change
    columns:
      - name: email_event_id
        tests:
          - unique
          - not_null
          - at_least_one

  - name: stg_hubspot__email_event_aggregates
    columns:
      - name: email_send_id
        tests:
          - unique
          - not_null
          - at_least_one

  - name: int__hubspot__email_sends
    columns:
      - name: event_id
        tests:
          - unique
          - not_null
          - at_least_one